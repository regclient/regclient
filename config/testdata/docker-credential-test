#!/bin/sh

registry_testhost='
{ "ServerURL": "testhost.example.com",
  "Username": "hello",
  "Secret": "world"
}
'

if [ "$1" = "get" ]; then
  read hostname
  case "$hostname" in
    testhost.example.com)
      echo "${registry_testhost}"
      exit 0
      ;;
  esac
fi
# unhandled request
exit 1
